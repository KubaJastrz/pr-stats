# pr-stats

Usage

```bash
npx pr-stats@latest
  --owner <owner>
  --repository <repository>
  [--groupByLabels=<string>]
  [--reload]

# Example
npx pr-stats --owner facebook --repository react --groupByLabels=dependencies,'good first issue'
```
